{% extends 'base.html.twig' %}

{% block title %} {{ post.title }} {% endblock %}

{% block body %}
    <div>
        <h2>Post</h2>
        <div>
            <div>
                <h3><a href="{{ path('blog_show', { 'id': id }) }}" >{{ id }} . {{ post.title }} </a></h3>
                <p>{{  post.text }}</p>
            </div>
        </div>
    </div>
{%  endblock %}
{% block javascripts %}{% endblock %}
