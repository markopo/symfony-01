{% extends 'base.html.twig' %}

{% block title %} Register {% endblock %}


{% block body %}
    <h3>{{ title }}</h3>
    <hr />
    <section>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </section>

{% endblock %}